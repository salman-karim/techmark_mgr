<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Techmark Manager</title>
    <link rel="stylesheet" type="text/css" href="../css/styles.css">
    <link href='http://fonts.googleapis.com/css?family=Source+Code+Pro:200,300,400,500' rel='stylesheet' type='text/css'>  </head>
  <body>

  <div class='loggedInUser'>  <% if current_user %>
      <%= current_user.username %>
      <form method="post" action="/session">
        <input type="hidden" name="_method" value="delete">
        <input class='signOutButton' type="submit" value="Sign out">
      </form>
    <% end %>
  </div>

    <% if flash[:errors] && !flash[:errors].empty? %>
      Please refer to the following errors below:
      <ul id='errors'>
        <% flash[:errors].each do |error| %>
          <li><%= error %></li>
        <% end %>
      </ul>
    <% end %>

    <%= yield %>

  </body>
</html>
